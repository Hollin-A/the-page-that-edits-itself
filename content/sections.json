{
  "sections": [
    {
      "id": "main-heading",
      "level": 1,
      "text": "The site that edits itself",
      "type": "heading",
      "visible": true
    },
    {
      "id": "elevator-pitch",
      "text": "Any visitor can suggest a change to this page. An AI moderates the suggestion, a second AI generates the edit, and the result becomes a real GitHub pull request that CI checks, auto-merges, and Vercel deploys — all within 30–90 seconds. No human in the loop.",
      "type": "paragraph",
      "visible": true
    },
    {
      "id": "hero-scene",
      "type": "threejs-scene",
      "height": 420,
      "camera": { "fov": 60, "position": [0, 0, 4] },
      "lights": [
        { "type": "AmbientLight", "intensity": 0.3 },
        { "type": "PointLight", "intensity": 3, "position": [5, 5, 5], "color": "#6366f1" },
        { "type": "PointLight", "intensity": 1.5, "position": [-5, -3, -3], "color": "#0ea5e9" }
      ],
      "objects": [
        {
          "id": "main-knot",
          "geometry": { "type": "TorusKnot", "params": [1, 0.3, 128, 16] },
          "material": { "type": "MeshPhysicalMaterial", "color": "#6366f1", "roughness": 0.15, "metalness": 0.85 },
          "animation": { "rotate": [0.002, 0.006, 0.001], "float": { "amplitude": 0.12, "speed": 0.8 } }
        }
      ],
      "visible": true
    },
    {
      "id": "pipeline-steps",
      "items": [
        "Suggestion received — your submission is rate-limited by IP and saved to the database with a status of \"queued\"",
        "Kill switch check — if the owner has paused the pipeline, your suggestion is stopped here before any AI is called",
        "Scope classification — Claude Haiku decides if your suggestion targets the clicked section only, or the whole page; section-scoped suggestions cannot modify other elements",
        "Moderation — Claude Haiku decides if the suggestion is safe, unsafe, or off-topic; anything not safe is rejected",
        "Generation — Claude Sonnet reads the current page content and your suggestion, then produces a structured edit targeting the right section",
        "Validation — the edit is checked against strict schemas; if anything is malformed or out of range, the suggestion is marked failed",
        "Commit — the edit is written to a new branch on GitHub and a pull request is opened automatically",
        "CI check — GitHub Actions checks every file in the pull request against an allowlist; any file outside the allowed paths blocks the merge",
        "Deploy — once merged, Vercel rebuilds the site; a GitHub Actions workflow then calls the deploy webhook, busts the cache, and pushes a live update to every open browser tab"
      ],
      "type": "ordered-list",
      "visible": true
    },
    {
      "body": "A GitHub Actions workflow blocks any pull request that touches files outside a strict allowlist (content/, theme/). The check runs on GitHub's infrastructure, independent of the agent pipeline. Even a malicious or malformed patch cannot reach application code — the CI wall is enforced at the repository level, not inside the AI models.",
      "id": "safety-callout",
      "title": "The agent cannot break the site",
      "tone": "info",
      "type": "callout",
      "visible": true
    },
    {
      "id": "safety-detail-heading",
      "level": 3,
      "text": "How the CI allowlist check actually works",
      "type": "heading",
      "visible": true
    },
    {
      "id": "safety-detail-paragraph",
      "text": "When Octokit opens a PR, GitHub Actions triggers a workflow that calls the GitHub API to fetch the full list of changed file paths in the diff. Each path is matched against the allowlist: content/ and theme/. If any single path falls outside those prefixes — whether introduced intentionally or by a malformed patch — the workflow exits with a non-zero code, the CI check fails, and GitHub's branch protection rules prevent auto-merge from proceeding. The Inngest step that enabled auto-merge has no ability to override this: the check runs on GitHub's infrastructure, not inside the agent's execution context.",
      "type": "paragraph",
      "visible": true
    },
    {
      "id": "safety-layers-heading",
      "level": 3,
      "text": "Other independent safety layers",
      "type": "heading",
      "visible": true
    },
    {
      "id": "safety-layers-list",
      "items": [
        "Rate limiting — 20 submissions/hour per GitHub-authenticated user, tracked by SHA-256 hashed IP stored in Supabase",
        "Scope classification — section-scoped suggestions only modify the clicked element; Claude Haiku explicitly marks all other sections read-only before passing context to Sonnet",
        "AI moderation gate — Claude Haiku screens every suggestion before any Sonnet call; unsafe or off-topic content is rejected at the cheapest step in the pipeline",
        "Zod schema validation — the agent's output is validated against strict schemas before any commit; bad shapes, missing required fields, and out-of-range values are all caught here",
        "Element locking — while a comment is in-flight, its target edit_id is locked; further submissions for the same element return HTTP 409 until the comment resolves",
        "Kill switch — reads the kill_switch row in the settings table; if active, the Inngest function halts before making any Anthropic API call",
        "Anthropic spend cap — a hard monthly limit configured in the Anthropic console; when reached, the API returns errors and the pipeline marks comments failed",
        "Held-comment queue — low-confidence outputs or sensitive edits are held for owner review; on approval, the patch is re-generated against current file state to avoid stale-patch merge conflicts"
      ],
      "type": "bullet-list",
      "visible": true
    },
    {
      "body": "The agent cannot invent new section types, write arbitrary HTML, or modify styling beyond the token schema. It calls update_sections with typed structured data that pre-registered renderer components know how to render. The renderer registry is hardcoded in application code — outside the agent's write scope entirely.",
      "id": "limits-callout",
      "title": "What the agent cannot do",
      "tone": "warn",
      "type": "callout",
      "visible": true
    },
    {
      "id": "what-structural-means",
      "text": "Structural edits are not limited to text substitution. The agent can split one section into multiple nodes, merge adjacent sections, reorder the sections array, insert a new typed section, change a section's type field (e.g. paragraph → callout), or set visible: false to hide a section — all by manipulating the JSON array that the renderer consumes.",
      "type": "paragraph",
      "visible": true
    },
    {
      "code": "agent(elevator-pitch): rewrite opening paragraph to active voice",
      "id": "commit-format",
      "language": "bash",
      "type": "code-block",
      "visible": true
    },
    {
      "id": "try-suggestions",
      "items": [
        "\"Split the pipeline steps into before and after the PR opens\"",
        "\"Add a section about what happens when a suggestion is rejected\"",
        "\"Merge the two safety sections into one\"",
        "\"Move the callout to just before the pipeline steps\"",
        "\"Rewrite the opening paragraph for a non-technical audience\""
      ],
      "type": "bullet-list",
      "visible": true
    },
    {
      "attribution": "docs/system-reference.md — on what this workflow actually does",
      "id": "capability-quote",
      "text": "A suggestion can split a paragraph into three, reorder sections, add a callout, or remove a section entirely. A pure database cannot rearrange page structure. Every change has a real GitHub PR, real CI, and a real deploy — because real work is happening at each step.",
      "type": "quote",
      "visible": true
    },
    {
      "attribution": "docs/system-reference.md",
      "id": "closing-quote",
      "text": "The thing is interesting because it's real, not because it claims to be.",
      "type": "quote",
      "visible": true
    },
    {
      "href": "https://github.com/Hollin-A/agentic-workflow-prototype/pulls",
      "id": "see-prs",
      "text": "Every change has a real pull request →",
      "type": "link-block",
      "visible": true
    }
  ]
}
